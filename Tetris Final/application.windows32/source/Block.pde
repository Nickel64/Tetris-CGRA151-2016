class Block {    //simple class to store the color and score of any position in the grid
  color blockColour;
  int scoreValue;

  public Block(color c, int score) {
    blockColour = c;
    scoreValue = score;
  }
}